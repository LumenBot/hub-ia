{
  "name": "strategie-ia-tests-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Tests d'intégration UI (Playwright) — composants front 06/produit-app/* et site-public/*. Périmètre distinct des tests pipeline RAG (09/tests/ pytest, owner Hub RAG).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "install:browsers": "playwright install --with-deps chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
